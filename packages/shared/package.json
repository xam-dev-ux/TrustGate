{
  "name": "@trustgate/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript types for TRUSTGATE",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "files": [
    "dist"
  ]
}
