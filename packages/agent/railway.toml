[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/agent/Dockerfile"

[deploy]
startCommand = "node packages/agent/dist/index.js"
healthcheckPath = "/api/accuracy"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
